microarch_level:
  - if: not(unix and x86_64)
    then:
      - 0
    else:
      # - 0
      - 1
      # - 3
      # Level 4 might not be available on the build machine
      # - 4
python:
  - "3.10"

pytorch:
  - 2.4.1
  # - 2.4.0
cuda:
  # - 11.8
  # - 12.1
  - 12.4.*
